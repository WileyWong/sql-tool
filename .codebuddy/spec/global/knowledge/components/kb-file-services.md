# 附件中台服务开发指南

> **文档版本**: v3.0.12  
> **维护团队**: 基础组件组  
> **接口人**: jeeliu(刘志杰)  
> **最后更新**: 2024-08-08  
> **状态**: ✅ 稳定运行中

---

## 📚 文档导航

本文档采用分卷模式,包含以下部分:

- [**主文档(当前)**](./kb-file-services.md) - 产品概述、快速开始、基础概念
- [**第一分卷**](./kb-file-services-part1.md) - 文件上传、下载、删除接口
- [**第二分卷**](./kb-file-services-part2.md) - 签名认证、预览、批量操作接口
- [**第三分卷**](./kb-file-services-part3.md) - 高级功能(图片处理、富文本、第三方集成)
- [**第四分卷**](./kb-file-services-part4.md) - Java SDK、最佳实践、常见问题

---

## 一、产品概述

### 1.1 核心能力

附件中台能力提供附件(文件)的一站式解决方案,旨在解决基础业务中涉及文件的存储与预览等主要问题。

**主要功能**:
- ✅ 文件存储(支持内网和外网访问)
- ✅ 在线预览(支持 PDF、Word、Excel、图片、富文本等)
- ✅ 文件下载(支持断点续传、批量下载)
- ✅ 加密存储(提供多种加密模式)
- ✅ 文件转换(Office文档转PDF)
- ✅ 图片处理(缩放、旋转、裁剪)
- ✅ 水印支持(多种水印模式)
- ✅ 访问权限控制(基于签名的权限校验)

**业务价值**:
- 🔒 为保密文件提供严格的加密存储方案
- 📄 支持多种文件类型的在线预览
- 🚀 提供完整的文件生命周期管理
- 🛡️ 通过签名机制保障文件访问安全

### 1.2 应用场景

- **薪酬系统**: 公司收入证明的加密存储与预览
- **招聘系统**: 入职薪资证明的安全管理
- **异动系统**: 敬业协议等文件的存储与下载
- **通用场景**: 任何需要文件上传、存储、预览、下载的业务系统

### 1.3 支持的文件类型

- 📄 **文档类**: PDF、Word(.doc/.docx)、Excel(.xls/.xlsx)、PPT
- 🖼️ **图片类**: JPG、PNG、GIF、BMP
- 📝 **富文本**: HTML格式
- 📦 **其他**: 支持任意格式文件的存储和下载

---

## 二、环境地址

### 2.1 内网环境

| 环境 | 域名 | 说明 |
|------|------|------|
| 测试环境 | `dev.ntsgw.oa.com` | 推荐使用 |
| 测试环境(旧) | ~~`demo.ntsgw.oa.com`~~ | 不建议使用 |
| 生产环境 | `ntsgw.oa.com` 或 `ntsgw.woa.com` | 推荐使用 |
| 生产环境(旧) | ~~`v2.ntsgw.oa.com`~~ | 不建议使用 |

**管理端地址**:
- OA-测试环境: https://dev-ntsapps.woa.com/fileservices/
- OA-生产环境: https://ntsapps.woa.com/fileservices/

**权限申请**:
- 测试环境: [权限中台申请](https://test-hrright.woa.com/apply/commonApplyNew/role?sysCode=hr-ms-fileservices&type=public)
- 生产环境: [权限中台申请](https://hrright.woa.com/apply/commonApplyNew/role?sysCode=hr-ms-fileservices&type=public)

### 2.2 外网环境

| 环境 | 后端域名 | 前端域名 |
|------|----------|----------|
| 测试环境 | `gateway.testihrtas.com` | `hrgw-{corpkey}.test-caagw.yunassess.com` |
| 生产环境 | `gateway.prodihrtas.com` | `hrgw-{corpkey}.ihr.tencent-cloud.com` |

**OSS网关访问** (用于IDC访问OA附件服务):
- OSS网关地址: `http://oss-esb.sdc.tencent.com`

---

## 三、前置条件

### 3.1 获取接入凭证

在调用附件服务前,需要先获取以下凭证:

- **appName**: 应用名称
- **appToken**: 应用令牌

> 📌 **获取方式**: 请参考 [签名计算方式文档](http://tapd.oa.com/HR_Platform/markdown_wikis/view/#1020394402011174689)

### 3.2 认证方式说明

附件服务支持两种认证方式:

#### 方式一: ESB认证 (后端调用推荐)

- **访问路径**: `/api/esb/hr-fileservices-load/{接口}`
- **认证机制**: 依赖API网关进行身份认证
- **优点**: 无需手动传递签名,调用简单
- **适用场景**: 后端服务间调用

#### 方式二: SSO认证 (前端调用推荐)

- **访问路径**: `/api/sso/hr-fileservices-load/{接口}`
- **认证机制**: 需要先调用 `getVisitSignature` 获取签名
- **必填参数**: `signature`
- **适用场景**: 前端页面调用、需要用户身份识别的场景

> ⚠️ **重要**: 采用 `/api/esb` 方式访问时不需要获取签名,也不需要在接口中传入 `signature` 参数

---

## 四、核心概念

### 4.1 fileUuid (文件唯一标识)

每个上传的文件都会分配一个唯一的 `fileUuid`,格式示例:
```
group1_M00/00/00/Cgxbel2xVRuABdIcAF-3psSc29w606.pdf
```

该ID用于文件的下载、预览、删除等所有操作。

### 4.2 签名机制 (signature)

**作用**:
- 身份认证: 识别调用者身份
- 权限控制: 限制可操作的文件范围
- 操作授权: 指定允许的操作类型(上传/下载/预览/删除等)
- 水印设置: 预设预览时的水印效果

**有效期**: 30分钟

**获取方式**: 调用 `getVisitSignature` 或 `getVisitSignatureExt` 接口

### 4.3 加密模式 (encryptMode)

附件服务支持三种加密模式:

| 模式 | 说明 | 适用场景 |
|------|------|----------|
| `normal` | 普通加密,系统自动生成密钥存储在KMS | 一般保密文件 |
| ~~`privately`~~ | ~~私有加密(已废弃)~~ | ~~已下线~~ |
| `strictly` | 严格加密,业务系统传递encryptToken | 薪酬等极高保密要求的文件 |

### 4.4 内外网同步规则

- ✅ 支持: 公网上传的文件可以在内网同步预览
- ❌ 不支持: 内网上传的文件在公网预览
- ⚠️ 限制: 公网上传的加密文件暂不支持在内网预览(密钥同步问题)

### 4.5 文件转换

**转换方向**: 
- Office文档(Word/Excel/PPT) → PDF

**触发方式**:
- 上传时指定 `convert=true` 参数
- 手动调用 `convertFile` 接口

**转换状态查询**: 调用 `getConvertResult` 接口

---

## 五、快速开始

### 5.1 后端调用(ESB认证)

#### 示例1: 上传文件

```java
// 引入依赖 (Maven)
<dependency>
    <groupId>com.tencent.hr</groupId>
    <artifactId>hr-fileservices-starter</artifactId>
    <version>2.0.5-SNAPSHOT</version>
</dependency>

// 配置文件 (application.yml)
hr:
  security:
    app-token: YOUR_APP_TOKEN
    app-name: YOUR_APP_NAME
  fileservices:
    enable: true

// Java代码
@Autowired
private FileServicesClient fileServicesClient;

public String uploadFile(MultipartFile file) {
    // 上传文件,返回fileUuid
    String fileUuid = fileServicesClient.upload(file);
    return fileUuid;
}
```

#### 示例2: 下载文件

```java
public File downloadFile(String fileUuid) {
    File file = fileServicesClient.downloadFile("", fileUuid);
    return file;
}
```

#### 示例3: 预览文件(获取预览URL)

```bash
# 直接访问预览接口
curl "http://ntsgw.oa.com/api/esb/hr-fileservices-view/viewOnline?fileUuid=group1_M00/00/00/xxx.pdf"
```

### 5.2 前端调用(SSO认证)

#### 步骤1: 获取签名

```javascript
// 调用后端接口获取签名
const signature = await axios.get('/api/esb/hr-fileservices-load/getVisitSignature', {
  params: {
    fileUuid: 'group1_M00/00/00/xxx.pdf',
    operate: 'visit', // 指定操作类型
    watermarkContent: '仅供内部使用', // 可选: 添加水印
    watermarkMode: 'darkgraysingle' // 可选: 水印模式
  }
});
```

#### 步骤2: 使用签名访问文件

```html
<!-- 预览文件 -->
<iframe 
  src="http://ntsgw.oa.com/api/sso/hr-fileservices-view/viewOnline?fileUuid=xxx&signature=xxx"
  width="100%" 
  height="600px">
</iframe>

<!-- 下载文件 -->
<a href="http://ntsgw.oa.com/api/sso/hr-fileservices-load/httpDownload?fileUuid=xxx&signature=xxx">
  下载文件
</a>
```

### 5.3 Python调用示例

```python
import requests

# 配置
BASE_URL = "http://ntsgw.oa.com/api/esb/hr-fileservices-load"
APP_NAME = "your_app_name"
APP_TOKEN = "your_app_token"

# 上传文件
def upload_file(file_path):
    url = f"{BASE_URL}/fileUpload"
    files = {'file': open(file_path, 'rb')}
    headers = {
        'appName': APP_NAME,
        'appToken': APP_TOKEN
    }
    
    response = requests.post(url, files=files, headers=headers)
    result = response.json()
    
    if result['success']:
        return result['data']['uuid']
    else:
        raise Exception(result['message'])

# 下载文件
def download_file(file_uuid, save_path):
    url = f"{BASE_URL}/httpDownload"
    params = {'fileUuid': file_uuid}
    
    response = requests.get(url, params=params, stream=True)
    
    with open(save_path, 'wb') as f:
        for chunk in response.iter_content(chunk_size=8192):
            f.write(chunk)

# 使用示例
file_uuid = upload_file('./document.pdf')
print(f"上传成功,fileUuid: {file_uuid}")

download_file(file_uuid, './downloaded.pdf')
print("下载成功")
```

---

## 六、通用请求头

所有接口调用时需要注意以下通用请求头:

| 请求头 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `caagw-corpkey` | String | 外网必填 | 租户信息,内网默认为`tencent`,外网由TAS网关传递 |
| `hrfile-encrypt-token` | String | 严格加密必填 | 严格加密模式下,用于获取KMS密钥进行文档加密 |

---

## 七、通用响应格式

### 7.1 成功响应

```json
{
  "success": true,
  "code": 0,
  "message": "操作成功",
  "data": {
    // 具体业务数据
  }
}
```

### 7.2 失败响应

```json
{
  "success": false,
  "code": 103,
  "message": "该文件上传失败",
  "data": null
}
```

### 7.3 常见状态码

| 状态码 | 说明 |
|--------|------|
| `0` | 操作成功 |
| `102` | 文件上传完成 |
| `103` | 文件上传失败 |
| `104` | 文件内容不存在(空文件) |
| `301` | 文件删除成功 |
| `302` | 文件删除失败 |
| `303` | 文件不存在 |

---

## 八、操作类型说明 (operate)

在获取签名时,可以通过 `operate` 参数限制签名的操作权限:

| 操作类型 | 说明 |
|----------|------|
| `upload` | 只能上传附件 |
| `visit` | 只能访问附件(下载和预览) |
| `view` | 只能预览,无下载权限 |
| `update` | 只能更新文件(如转换文件、图片处理) |
| `auth` | 只能进行附件授权操作 |
| `delete` | 只能删除附件 |
| `tags` | 只能添加或删除标签 |
| `docsqqmanager` | 注册腾讯文档管理员(公网暂不支持) |
| `all` | 拥有所有操作权限 |

---

## 九、水印模式说明 (watermarkMode)

附件预览时支持添加文字水印,提供以下六种模式:

| 模式 | 说明 |
|------|------|
| `lightcoralmulti` | 淡珊瑚色多个水印 |
| `lightcoralsingle` | 淡珊瑚色单个水印 |
| `darkgraysingle` | 深灰色单个水印(默认) |
| `darkgraymulti` | 深灰色多个水印 |
| `weakgraysingle` | 淡灰色单个水印 |
| `weakgraymulti` | 淡灰色多个水印 |

**统一样式**:
- 字体: 黑体
- 旋转角度: 45°
- 透明度: 0.5

**背景水印** (仅PDF):
- 参数: `watermarkBackground=true`
- 效果: 水印置于文档下层,不遮挡文字
- 注意: 如果PDF存在底色,水印可能不可见

---

## 十、重要限制与说明

### 10.1 内外网访问限制

- ✅ 公网上传的文件可以在内网同步预览
- ❌ 内网上传的文件不支持在公网预览
- ⚠️ 公网上传的加密文件暂不支持在内网预览

### 10.2 文件大小限制

- 断点续传: 每个分片需要在 **5MB 以上**
- 建议: 大文件上传使用断点续传功能

### 10.3 富文本限制

富文本类型的文件有以下限制:
- ❌ 不支持加密
- ❌ 不支持断点续传
- ❌ 不支持图片处理
- ✅ 仅提供后端访问接口

### 10.4 存储类型 (storeType)

内网上传文件时可以指定存储类型:

| 值 | 说明 |
|----|------|
| `intranet` | 仅支持内网查看(默认) |
| `extranet` | 支持外网查看 |

---

## 十一、版本更新记录

| 日期 | 版本 | 更新人 | 说明 |
|------|------|--------|------|
| 2024-08-08 | 3.0.12 | jeeliu | fastdfs存储组件下线,存储组件统一到COS服务 |

**Java SDK版本**:
- 最新版本: `2.0.5-SNAPSHOT`
- 更新内容: 新增文件删除接口、支持设置租户信息

---

## 十二、下一步

请继续阅读分卷文档以了解详细的接口使用方法:

1. [**第一分卷**](./kb-file-services-part1.md) - 文件上传、下载、删除接口
2. [**第二分卷**](./kb-file-services-part2.md) - 签名认证、预览、批量操作接口
3. [**第三分卷**](./kb-file-services-part3.md) - 高级功能(图片处理、富文本、第三方集成)
4. [**第四分卷**](./kb-file-services-part4.md) - Java SDK、最佳实践、常见问题

---

## 技术支持

- **接口人**: jeeliu(刘志杰)
- **管理端**: [https://ntsapps.woa.com/fileservices/](https://ntsapps.woa.com/fileservices/)
- **权限申请**: [权限中台](https://hrright.woa.com/apply/commonApplyNew/role?sysCode=hr-ms-fileservices&type=public)
