<!--
================== AI 处理指引 ==================

【文档提取规则】
1. 方法统计：统计所有 public 方法，区分静态方法和实例方法
2. 类型判断：
   - 纯静态工具：所有方法为 static，无 @Component/@Service 注解
   - Spring Bean 工具：有 @Component/@Service 注解，通过依赖注入使用
   - 有状态工具：使用 ThreadLocal 或实例变量存储状态
3. 线程安全判断：
   - 安全：纯静态无状态方法 / ThreadLocal 隔离
   - 需注意：有共享可变状态 / 非线程安全的成员变量
4. 依赖获取方式：无依赖 / @Autowired 注入 / SpringUtil.getBean() 延迟获取
5. 可选章节：内容为空时整章省略（含标题），不保留空表格

【代码生成规则】
1. 纯静态工具：public final class + private 构造器 + static 方法
2. Spring Bean 工具：@Component + @Autowired 依赖注入
3. 有状态工具：ThreadLocal<T> + 提供 clear() 方法清理上下文
4. 异常处理：工具类通常抛出 RuntimeException 或自定义业务异常
5. 日志：使用 @Slf4j 注解，关键操作记录日志

**注意**: 以上为通用规则，若实际项目代码风格与规则不一致，以实际项目为准。

================================================
-->

# {{CLASS_NAME}}

> **路径**: `{{PACKAGE_NAME}}.{{CLASS_NAME}}` <!-- [必填] -->  
> **类型**: Utils <!-- [必填] -->  
> **工具类型**: {{UTIL_TYPE}} <!-- 纯静态 / Spring Bean / 有状态 -->  
> **线程安全**: {{THREAD_SAFE}} <!-- 是 / 否 / 条件安全(需说明) -->  
> **职责**: {{RESPONSIBILITY}} <!-- [必填] -->  
> **代码行数**: {{LOC}} 行（SLOC） <!-- [必填] -->

---

## 📊 统计信息

| 方法总数 | 静态方法 | 实例方法 | 公开方法 | 私有方法 |
|:--------:|:--------:|:--------:|:--------:|:--------:|
| {{TOTAL_METHOD}} | {{STATIC_COUNT}} | {{INSTANCE_COUNT}} | {{PUBLIC_COUNT}} | {{PRIVATE_COUNT}} |

---

## 📋 方法完整列表

> **重要**: 必须列出工具类中定义的**全部公开方法**，不可遗漏任何方法签名。

### 方法清单

| # | 方法名 | 静态 | 返回类型 | 说明 |
|:-:|--------|:----:|----------|------|
| 1 | `{{METHOD_NAME_1}}` | {{STATIC_1}} | `{{RETURN_TYPE_1}}` | {{DESC_1}} |
| 2 | `{{METHOD_NAME_2}}` | {{STATIC_2}} | `{{RETURN_TYPE_2}}` | {{DESC_2}} |
| ... | ... | ... | ... | ... |

---

### {{METHOD_NAME}}

| 属性 | 值 |
|------|-----|
| 签名 | `{{MODIFIER}} {{RETURN_TYPE}} {{METHOD_NAME}}({{PARAMS_BRIEF}})` |
| 静态 | {{IS_STATIC}} |
| 说明 | {{DESCRIPTION}} |

**参数**:

| 参数 | 类型 | 注解 | 说明 |
|------|------|------|------|
| {{PARAM_NAME}} | `{{PARAM_TYPE}}` | `{{PARAM_ANNOTATION}}` | {{PARAM_DESC}} |

**返回**: `{{RETURN_TYPE}}` - {{RETURN_DESC}}

**异常**: <!-- [可选] 如无异常可省略 -->

| 异常类型 | 触发条件 |
|----------|----------|
| `{{EXCEPTION_TYPE}}` | {{EXCEPTION_CONDITION}} |

<!-- 重复以上结构，直到列出全部公开方法 -->

---

## 🔗 依赖组件（我依赖谁）

<!-- [可选] 纯静态无状态工具类通常无依赖，可省略此章节 -->

| 组件 | 类型 | 获取方式 | 用途 |
|------|------|----------|------|
| {{DEP_NAME}} | {{DEP_TYPE}} | {{DEP_OBTAIN}} | {{DEP_PURPOSE}} |

*获取方式示例：@Autowired / SpringUtil.getBean() / 构造器注入*

*如无章节内容，此章节可省略*

---

## 🔙 被依赖方（谁依赖我）

<!-- [可选] 如无法确定被依赖方，可省略此章节 -->

| 组件 | 类型 | 调用方法 |
|------|------|----------|
| {{CALLER_NAME}} | {{CALLER_TYPE}} | {{CALLED_METHODS}} |

**影响分析**: 修改本类可能影响 {{IMPACT_COUNT}} 个组件

*如无章节内容，此章节可省略*

---

## 🏷️ 自定义注解

<!-- [可选] 仅列出项目自定义注解或特殊框架注解，@Slf4j 等标准注解无需列出 -->

| 注解 | 作用范围 | 说明 |
|------|----------|------|
| `{{ANNOTATION}}` | {{SCOPE}} | {{ANNOTATION_DESC}} |

*如无章节内容，此章节可省略*

---

## 💻 核心业务逻辑

<!-- [可选] 选择标准：复杂算法、关键处理流程、状态管理逻辑。如无复杂逻辑可省略此章节 -->

```java
{{METHOD_MODIFIER}} {{RETURN_TYPE}} {{METHOD_NAME}}({{PARAMS}}) {
    // 1. {{STEP_1}}
    // 2. {{STEP_2}}
    return {{RETURN_STATEMENT}};
}
```

*如无章节内容，此章节可省略*

---

## 💡 使用示例

<!-- [可选] 提供典型使用场景的代码示例 -->

```java
{{USAGE_EXAMPLE}}
```

*如无章节内容，此章节可省略*

---

## 📎 关联文档

<!-- [可选] 引用相关的数据结构和服务文档 -->

| 文档类型 | 文档名称 | 说明 |
|----------|----------|------|
| Entity | [{{ENTITY_NAME}}](./entity/{{ENTITY_FILE}}.md) | {{ENTITY_DESC}} |
| Service | [{{SERVICE_NAME}}](./service/{{SERVICE_FILE}}.md) | {{SERVICE_DESC}} |

*如无章节内容，此章节可省略*

---

## ⚠️ 注意事项

<!-- 根据实际情况填写，以下为占位符 -->

- {{NOTICE_1}}
- {{NOTICE_2}}
- {{NOTICE_3}}

*示例：*
- *所有方法均为静态方法，无需实例化*
- *使用 ThreadLocal 存储上下文，需在请求结束时调用 clear() 清理*
- *依赖 Spring 容器，需确保 Spring 上下文已初始化后调用*
- *非线程安全，多线程环境需外部同步*
- *部分方法有缓存，修改数据后需手动刷新*

*如无章节内容，此章节可省略*

---

## 📝 维护记录

| 时间 | 维护人 | 维护内容 | 版本 |
|------|--------|----------|------|
| {{SCAN_DATE}} | AI自动生成 | 初始创建文档 | v1.0 |
