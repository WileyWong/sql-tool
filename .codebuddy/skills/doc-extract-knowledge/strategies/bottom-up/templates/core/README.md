# 核心模板 (Core Templates)

19 个类型的文档模板 + 1 个兜底模板 + 2 个索引/架构模板，每个类生成独立的 `.md` 文件。

## 模板列表

| 序号 | 模板文件 | 输出目录 | 优先级 |
|------|----------|----------|--------|
| - | `directory-index.md` | `kb/README.md` | - |
| - | `architecture.md` | `kb/architecture.md` | - |
| - | `default.md` | `kb/other/` | 兜底 |
| 1 | `interface.md` | `interface/` | P0 |
| 2 | `abstract.md` | `abstract/` | P0 |
| 3 | `controller.md` | `controller/` | P0 |
| 4 | `service.md` | `service/` | P0 |
| 5 | `mapper.md` | `mapper/` | P0 |
| 6 | `entity.md` | `entity/` | P1 |
| 7 | `dto.md` | `dto/` | P1 |
| 8 | `vo-response.md` | `vo-response/` | P1 |
| 9 | `request.md` | `request/` | P1 |
| 10 | `exception.md` | `exception/` | P1 |
| 11 | `enum.md` | `enum/` | P1 |
| 12 | `constants.md` | `constants/` | P2 |
| 13 | `feign.md` | `feign/` | P2 |
| 14 | `config.md` | `config/` | P2 |
| 15 | `handler.md` | `handler/` | P2 |
| 16 | `job-task.md` | `job-task/` | P2 |
| 17 | `mq-listener.md` | `mq-listener/` | P2 |
| 18 | `utils.md` | `utils/` | P2 |
| 19 | `annotation.md` | `annotation/` | P2 |

## 使用方式

每个模板用于生成对应类型的独立文件：
- 输入: `UserController.java`
- 输出: `kb/controller/UserController.md`

## 模板必含章节

每个类模板都包含以下标准章节：

1. **头部信息**: 路径、类型、职责、代码行数（SLOC）
2. **📊 统计信息**: 类型特定的统计表格
3. **📋 方法/接口列表**: 详细方法信息（含完整签名）
4. **🔗 依赖组件（我依赖谁）**: 本类依赖的其他组件
5. **🔙 被依赖方（谁依赖我）**: 哪些组件依赖本类
6. **🏷️ 关键注解**: 类上的关键注解

## 方法签名规范

**所有方法必须记录完整签名，包含：**

| 元素 | 示例 | 必须 |
|------|------|:----:|
| 方法注解 | `@GetMapping`, `@Transactional`, `@Async` | ✅ |
| 访问修饰符 | `public`, `protected`, `private` | ✅ |
| 返回类型（含泛型） | `List<UserVO>`, `ResponseEntity<Result<T>>` | ✅ |
| 方法名 | `getUserById`, `createUser` | ✅ |
| 参数列表（含注解） | `@PathVariable("id") Long id` | ✅ |
| 异常声明 | `throws BusinessException` | ✅ |

**方法签名格式**:
```java
@GetMapping("/{id}")
@PreAuthorize("hasRole('USER')")
public ResponseEntity<Result<UserVO>> getUserById(
    @PathVariable("id") @Min(1) Long id,
    @RequestParam(value = "fields", required = false) String fields
) throws ResourceNotFoundException
```

**参数说明表格**:
| 参数 | 类型 | 注解 | 必填 | 默认值 | 说明 |
|------|------|------|:----:|--------|------|
| id | Long | `@PathVariable("id") @Min(1)` | 是 | - | 用户ID |
| fields | String | `@RequestParam(required = false)` | 否 | - | 返回字段 |

## 模板变量格式

所有模板使用统一的 `{{VARIABLE}}` 变量格式：
- `{{CLASS_NAME}}` - 类名
- `{{PACKAGE_NAME}}` - 包名
- `{{RESPONSIBILITY}}` - 职责描述
- `{{LOC}}` - 代码行数（SLOC）
- `{{DATE}}` - 日期
- `{{AUTHOR}}` - 作者
- `{{METHOD_ANNOTATIONS}}` - 方法注解
- `{{PARAMS_WITH_ANNOTATIONS}}` - 带注解的参数列表
- `{{THROWS_CLAUSE}}` - 异常声明

## 模板说明

### interface.md
用于接口定义，记录：
- 泛型参数
- 方法签名（含参数注解）
- 默认方法
- 实现类列表

### abstract.md
用于抽象类，记录：
- 抽象方法（子类必须实现）
- 模板方法（可被覆盖）
- 具体方法（子类继承）
- 子类列表

### controller.md
用于 REST 控制器，记录：
- 接口路径和 HTTP 方法
- 完整方法签名（含 @RequestMapping 等）
- 参数注解（@PathVariable, @RequestParam 等）
- 响应类型

### service.md
用于业务服务，记录：
- 事务配置
- 完整方法签名（含 @Transactional 等）
- 缓存注解
- 异常处理

### mapper.md
用于数据访问，记录：
- SQL 方法签名（含 @Param 等）
- 关联 XML 配置
- 动态 SQL 说明

### constants.md
用于常量类，记录：
- 常量分组
- 完整常量定义
- 被依赖分析

### exception.md
用于自定义异常类，记录：
- 继承关系
- 自定义字段
- 构造方法签名
- 使用场景

### enum.md
用于枚举类，记录：
- 所有枚举值及其属性
- 自定义字段和方法
- 状态流转图（如适用）

### architecture.md
用于生成项目架构文档，记录：
- 分层架构图
- 模块依赖矩阵
- 循环依赖检测
- 核心调用链

### default.md（兜底模板）
当类无法匹配任何已知类型时使用的通用模板：
- 适用于无法识别类型的 Java 类
- 包含完整的标准章节结构
- 输出目录为 `kb/other/`
- 确保所有类都能生成文档，不会遗漏
