# 自定义注解索引

> **覆盖范围**: `{{BASE_PACKAGE}}` 下所有 `@interface` 定义  
> **文件总数**: {{ANNOTATION_COUNT}}个  
> **代码总行数**: {{ANNOTATION_LOC}} 行  
> **生成时间**: {{SCAN_DATE}}

---

## 一、架构概览

### 目录结构
```
annotation/
├── Log - 日志记录注解
├── RateLimit - 限流注解
├── RequirePermission - 权限校验注解
└── ...
```

### 按功能分类
| 功能 | 注解数量 | 用途 |
|------|---------|------|
| 日志 | 1 | 方法调用日志 |
| 限流 | 1 | 接口限流 |
| 权限 | 1 | 权限校验 |

---

## 二、详细清单

### Log - 日志记录注解

**类路径**: `com.company.project.annotation.Log`  
**代码行数**: 18 行（SLOC）

#### 元注解信息
- **@Target**: `ElementType.METHOD`
- **@Retention**: `RetentionPolicy.RUNTIME`
- **@Documented**: 是

#### 属性列表
| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| value | String | "" | 操作描述 |
| module | String | "" | 模块名称 |
| recordParams | boolean | true | 是否记录参数 |
| recordResult | boolean | false | 是否记录返回值 |

**关联切面**: `LogAspect`

**使用示例**:
```java
@Log(value = "查询用户", module = "用户管理")
public UserVO getUserById(Long id) { ... }
```

---

### RateLimit - 限流注解

**类路径**: `com.company.project.annotation.RateLimit`  
**代码行数**: 15 行（SLOC）

#### 元注解信息
- **@Target**: `ElementType.METHOD`
- **@Retention**: `RetentionPolicy.RUNTIME`

#### 属性列表
| 属性 | 类型 | 默认值 | 说明 |
|------|------|--------|------|
| limit | int | 100 | 每秒最大请求数 |
| timeout | int | 1 | 超时时间（秒） |

**关联切面**: `RateLimitAspect`

---

## 📚 相关文档

- [Common公共类索引](./common.md)
- [Handler处理器索引](./handler.md)

---

## 📝 维护记录

| 时间 | 维护人 | 维护内容 | 版本 |
|------|--------|----------|------|
| {{SCAN_DATE}} | AI自动生成 | 初始创建文档 | v1.0 |
