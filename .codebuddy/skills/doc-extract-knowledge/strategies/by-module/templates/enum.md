<!--
================== AI 处理指引 ==================

【文档提取规则】
1. 枚举值提取：列出所有枚举常量及其字段值
2. 自定义字段：提取枚举类中定义的字段（code、desc 等）
3. 自定义方法：提取 getByCode、getDescription 等方法
4. 状态流转：如果是状态枚举，分析状态流转规则
5. 可选章节：内容为空时整章省略（含标题），不保留空表格

【代码生成规则】
1. 枚举类：public enum + 私有字段 + 构造器 + getter
2. 查找方法：public static Xxx getByCode(int code)
3. 状态枚举：可增加 canTransitionTo() 方法
4. JSON 序列化：@JsonValue 标记序列化字段

**注意**: 以上为通用规则，若实际项目代码风格与规则不一致，以实际项目为准。

================================================
-->

# {{CLASS_NAME}}

> **路径**: `{{PACKAGE_NAME}}.{{CLASS_NAME}}` <!-- [必填] -->  
> **类型**: Enum <!-- [必填] -->  
> **职责**: {{RESPONSIBILITY}} <!-- [必填] -->  
> **代码行数**: {{LOC}} 行（SLOC） <!-- [必填] -->

---

## 📊 统计信息

| 枚举值数量 | 自定义字段数 | 自定义方法数 |
|:----------:|:------------:|:------------:|
| {{ENUM_VALUE_COUNT}} | {{FIELD_COUNT}} | {{METHOD_COUNT}} |

---

## 📋 枚举值列表

> **重要**: 必须列出枚举类中定义的**全部枚举值**，不可遗漏。

| # | 枚举值 | {{FIELD_1}} | {{FIELD_2}} | 说明 |
|:-:|--------|-------------|-------------|------|
| 1 | `{{ENUM_VALUE_1}}` | `{{VALUE_1_1}}` | `{{VALUE_1_2}}` | {{DESC_1}} |
| 2 | `{{ENUM_VALUE_2}}` | `{{VALUE_2_1}}` | `{{VALUE_2_2}}` | {{DESC_2}} |
| ... | ... | ... | ... | ... |

---

## 📋 自定义字段

<!-- [可选] 如无自定义字段，可省略此章节 -->

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `{{FIELD_NAME}}` | `{{FIELD_TYPE}}` | {{FIELD_DESC}} |

*如无章节内容，此章节可省略*

---

## 📋 构造方法

```java
{{CLASS_NAME}}({{CONSTRUCTOR_PARAMS}}) {
    {{CONSTRUCTOR_BODY}}
}
```

---

## 📋 自定义方法

<!-- [可选] 如无自定义方法，可省略此章节 -->

### 方法清单

| # | 方法名 | 静态 | 返回类型 | 说明 |
|:-:|--------|:----:|----------|------|
| 1 | `getByCode` | 是 | `{{CLASS_NAME}}` | 根据编码获取枚举值 |
| 2 | `getCode` | 否 | `{{CODE_TYPE}}` | 获取枚举编码 |
| 3 | `getDescription` | 否 | `String` | 获取枚举描述 |
| ... | ... | ... | ... | ... |

### getByCode

| 属性 | 值 |
|------|-----|
| 签名 | `public static {{CLASS_NAME}} getByCode({{CODE_TYPE}} code)` |
| 说明 | 根据编码获取枚举值，未找到返回 null |

**参数**:

| 参数 | 类型 | 说明 |
|------|------|------|
| code | `{{CODE_TYPE}}` | 枚举编码 |

**返回**: `{{CLASS_NAME}}` - 对应的枚举值

*如无章节内容，此章节可省略*

---

## 🔗 依赖组件（我依赖谁）

<!-- 枚举类通常无依赖 -->

无（枚举类通常无依赖）

---

## 🔙 被依赖方（谁依赖我）

| 组件 | 类型 | 使用场景 |
|------|------|----------|
| {{CALLER_NAME}} | {{CALLER_TYPE}} | {{USAGE}} |

**影响分析**: 修改本类可能影响 {{IMPACT_COUNT}} 个组件

---

## 🏷️ 自定义注解

<!-- [可选] 仅列出项目自定义注解 -->

| 注解 | 作用范围 | 说明 |
|------|----------|------|
| `{{ANNOTATION}}` | {{SCOPE}} | {{ANNOTATION_DESC}} |

*如无章节内容，此章节可省略*

---

## 💡 使用示例

```java
// 获取枚举值
{{CLASS_NAME}} value = {{CLASS_NAME}}.{{EXAMPLE_VALUE}};

// 根据编码获取
{{CLASS_NAME}} value = {{CLASS_NAME}}.getByCode({{EXAMPLE_CODE}});

// 获取属性
{{CODE_TYPE}} code = value.getCode();
String desc = value.getDescription();

// 状态判断
if (entity.getStatus() == {{CLASS_NAME}}.{{EXAMPLE_VALUE}}) {
    // 处理逻辑
}
```

---

## 🔄 状态流转图

<!-- [可选] 仅状态枚举需要此章节 -->

```
{{STATE_DIAGRAM}}
```

*示例：*
```
PENDING(待处理) → PROCESSING(处理中) → COMPLETED(已完成)
                                    ↘ REJECTED(已拒绝)
```

*如无章节内容，此章节可省略*

---

## 📝 业务规则

<!-- [可选] 记录关键业务规则和约束 -->

- {{BUSINESS_RULE_1}}
- {{BUSINESS_RULE_2}}
- {{BUSINESS_RULE_3}}

*示例：*
- *状态流转：待处理(0) → 处理中(1) → 已完成(2) → 已拒绝(3)*
- *已完成/已拒绝状态不可变更*
- *处理中状态可回退到待处理*

*如无章节内容，此章节可省略*

---

## 📝 维护记录

| 时间 | 维护人 | 维护内容 | 版本 |
|------|--------|----------|------|
| {{SCAN_DATE}} | AI自动生成 | 初始创建文档 | v1.0 |
