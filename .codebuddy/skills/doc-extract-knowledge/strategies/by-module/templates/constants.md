<!--
================== AI 处理指引 ==================

【文档提取规则】
1. 常量分组：按业务含义分组（状态码、错误码、配置键等）
2. 常量类型：区分字符串常量、数值常量、其他类型
3. 完整定义：记录完整的常量定义语句
4. 使用场景：从被依赖方分析常量的使用场景
5. 可选章节：内容为空时整章省略（含标题），不保留空表格

【代码生成规则】
1. 常量类：public final class + private 构造器
2. 常量定义：public static final TYPE NAME = value;
3. 分组：使用内部类或注释分组
4. 命名：全大写 + 下划线分隔

**注意**: 以上为通用规则，若实际项目代码风格与规则不一致，以实际项目为准。

================================================
-->

# {{CLASS_NAME}}

> **路径**: `{{PACKAGE_NAME}}.{{CLASS_NAME}}` <!-- [必填] -->  
> **类型**: Constants <!-- [必填] -->  
> **职责**: {{RESPONSIBILITY}} <!-- [必填] -->  
> **代码行数**: {{LOC}} 行（SLOC） <!-- [必填] -->

---

## 📊 统计信息

| 常量总数 | 字符串常量 | 数值常量 | 其他类型 | 分组数 |
|:--------:|:----------:|:--------:|:--------:|:------:|
| {{TOTAL_COUNT}} | {{STRING_COUNT}} | {{NUMBER_COUNT}} | {{OTHER_COUNT}} | {{GROUP_COUNT}} |

---

## 📋 常量分组

> **重要**: 必须列出常量类中定义的**全部常量**，不可遗漏。

### {{GROUP_NAME_1}}

| # | 常量名 | 类型 | 值 | 说明 |
|:-:|--------|------|-----|------|
| 1 | `{{CONSTANT_NAME_1}}` | `{{TYPE_1}}` | `{{VALUE_1}}` | {{DESC_1}} |
| 2 | `{{CONSTANT_NAME_2}}` | `{{TYPE_2}}` | `{{VALUE_2}}` | {{DESC_2}} |
| ... | ... | ... | ... | ... |

### {{GROUP_NAME_2}}

| # | 常量名 | 类型 | 值 | 说明 |
|:-:|--------|------|-----|------|
| 1 | `{{CONSTANT_NAME}}` | `{{TYPE}}` | `{{VALUE}}` | {{DESCRIPTION}} |

<!-- 重复以上结构，直到列出全部分组 -->

---

## 📋 错误码常量

<!-- [可选] 如非错误码常量类，可省略此章节 -->

| 错误码 | 常量名 | 错误消息 | 使用场景 |
|:------:|--------|----------|----------|
| `{{CODE}}` | `{{ERROR_CODE_NAME}}` | {{MESSAGE}} | {{SCENARIO}} |

*如无章节内容，此章节可省略*

---

## 📋 配置常量

<!-- [可选] 如非配置常量类，可省略此章节 -->

| 常量名 | 值 | 说明 | 关联配置 |
|--------|-----|------|----------|
| `{{CONFIG_NAME}}` | `{{VALUE}}` | {{DESCRIPTION}} | {{RELATED_CONFIG}} |

*如无章节内容，此章节可省略*

---

## 🔙 被依赖方（谁依赖我）

| 组件 | 类型 | 使用的常量 |
|------|------|------------|
| {{CALLER_NAME}} | {{CALLER_TYPE}} | {{USED_CONSTANTS}} |

**影响分析**: 修改本类常量可能影响 {{IMPACT_COUNT}} 个组件

---

## 🏷️ 自定义注解

<!-- [可选] 仅列出项目自定义注解 -->

| 注解 | 作用范围 | 说明 |
|------|----------|------|
| `{{ANNOTATION}}` | {{SCOPE}} | {{ANNOTATION_DESC}} |

*如无章节内容，此章节可省略*

---

## ⚠️ 注意事项

- 常量值变更需评估影响范围
- 错误码变更需同步更新文档
- 配置常量变更需检查环境配置

---

## 💡 使用示例

```java
// 直接引用常量
public class {{SERVICE_NAME}} {
    
    public void example() {
        // 使用状态常量
        if (status == {{CLASS_NAME}}.{{STATUS_CONSTANT}}) {
            // 处理逻辑
        }
        
        // 使用错误码常量
        throw new BusinessException({{CLASS_NAME}}.{{ERROR_CODE}}, {{CLASS_NAME}}.{{ERROR_MSG}});
        
        // 使用配置常量
        String key = {{CLASS_NAME}}.{{CONFIG_KEY}};
    }
}
```

---

## 📝 维护记录

| 时间 | 维护人 | 维护内容 | 版本 |
|------|--------|----------|------|
| {{SCAN_DATE}} | AI自动生成 | 初始创建文档 | v1.0 |
