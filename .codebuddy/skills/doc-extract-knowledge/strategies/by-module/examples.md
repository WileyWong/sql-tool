# 使用示例

本文档展示 `doc-extract-by-module` 技能的实际使用场景和执行流程。

## 示例1：为用户模块生成文档（新项目）

### 场景描述

Spring Boot 项目的用户管理模块，包含 45 个 Java 文件，需要生成完整的知识库文档。

### 用户请求

```
用户：为 src/main/java/com/xxx/user 模块生成文档
```

### 执行流程

#### 1. 参数确认

```

🎯 参数确认

源代码目录: src/main/java/com/xxx/user ✅
文档输出目录: kb/user （默认）

✅ 开始执行

```

#### 2. 建立文件索引

```

📋 阶段1：建立文件索引

检查索引文件: kb/user/.file-list.md ... ❌ 不存在

📂 扫描模块: src/main/java/com/xxx/user

扫描方式: list_files 工具（递归扫描）
扫描到的文件数: 45 个
创建索引文件: kb/user/.file-list.md ✅

✅ 文件索引建立完成

```

#### 3. 按3个一批生成文档

**处理第 1 批（文件1-3）**：

```
📄 处理第1批（文件1-3）
🔄 立即更新索引文件（批量更新文件1-3）...
✅ 第1批（文件1-3）处理完成

```

**处理第 2 批（文件4-6）**：

```
✅ 第2批（文件4-6）处理完成
```

**自动连续处理**：

```
📊 总体进度
已完成: 6/45 (13.3%)，继续处理下一批3个文件（文件7-9）...
```

#### 4. 阶段3：生成目录索引与完成总结

**步骤3.1：询问是否生成 README.md**

```

📋 阶段3：生成目录索引

所有 45 个文件的文档已生成完成！

是否为文档目录生成 README.md 索引文件？

选项：
  [1] 生成 README.md（如已存在则更新）
  [2] 跳过

请选择（1/2）：

```

> **🔴 此时必须停止并等待用户选择！**

**用户选择 1（生成）**：

```
📂 扫描文档目录: kb/user/
扫描到的文档文件: 45 个（排除 .file-list.md 和 README.md）
读取模板: templates/directory-index.md ✅
生成索引文件...
保存文件: kb/user/README.md ✅
✅ 目录索引生成完成
```

**用户选择 2（跳过）**：

```

⏭️ 跳过 README.md 生成

如需后续生成，可执行：
  "为 kb/user/ 目录生成 README.md 索引"

```

**步骤3.3：输出完成总结**

```

🎉 文档生成完成

模块: src/main/java/com/xxx/user
文档目录: kb/user/

📊 统计信息:
  - 总文件数: 45
  - 已生成文档: 45
  - 文档总大小: 256.7KB
  - README.md: ✅ 已生成 / ⏭️ 已跳过
  
📂 文档分布:
  - controller/: 5 个文档 (23.5KB)
  - service/: 8 个文档 (58.2KB)
  - mapper/: 6 个文档 (32.1KB)
  - entity/: 10 个文档 (45.9KB)
  - dto/: 9 个文档 (38.4KB)
  - utils/: 7 个文档 (58.6KB)

📋 生成的文件:
  - 索引文件: kb/user/.file-list.md
  - 目录索引: kb/user/README.md（如已生成）
  - 文档文件: 45 个

✅ 所有文档已保存到 kb/user/ 目录

```

---

## 示例2：断点续传（会话中断后继续）

### 场景描述

用户在生成过程中会话意外中断（如IDE崩溃、网络断开），稍后重新执行时自动从断点继续。

### 第一次执行（处理到28个后会话中断）

```
📊 总体进度
已完成: 28/45 (62.2%)，继续处理下一批3个文件...
```

💥 **此时会话意外中断**（IDE崩溃/网络断开）

索引文件已自动保存进度：
- 文件1-28：状态为"已生成"✅
- 文件29-45：状态为"待创建"⏳

### 第二次执行（自动从断点继续）

```
用户：继续为 user 模块生成文档
```

```

🔍 检测到未完成的任务

索引文件: kb/user/.file-list.md ✅ 存在

进度统计:
  - 已完成: 28 个 (62.2%)
  - 待处理: 17 个 (37.8%)
  
下一批文件:
  - 序号: 29-31
  - 文件: dto/UserQueryDTO.java 等3个
  - 状态: 待创建

⚠️ 检测到未完成的任务，将从断点继续执行

✅ 自动从文件 29 开始处理（每批3个）

```

```

📄 处理第10批（文件29-31）

生成文档...\n  

🔄 立即更新索引文件（批量更新文件29-31）...
  保存索引: kb/user/.file-list.md ✅

✅ 第10批（文件29-31）处理完成

（自动继续处理剩余文件...）
```

#### 4. 阶段3：生成目录索引与完成总结

**步骤3.1：询问是否生成 README.md**

```
📋 阶段3：生成目录索引

所有 45 个文件的文档已生成完成！

是否为文档目录生成 README.md 索引文件？

选项：
  [1] 生成 README.md（如已存在则更新）
  [2] 跳过

请选择（1/2）：
```

> **🔴 此时必须停止并等待用户选择！**

（用户选择后执行相应操作）

**步骤3.3：输出完成总结**

```
🎉 文档生成完成

模块: src/main/java/com/xxx/user
文档目录: kb/user/

📊 统计信息:
  - 总文件数: 45
  - 已生成文档: 45（本次新增17个）
  - README.md: ✅ 已生成 / ⏭️ 已跳过

✅ 所有文档已保存到 kb/user/ 目录
```

---

## 示例3：代码变更后重新执行（新增文件）

### 场景描述

用户已生成部分文档，后来在源码中新增了 3 个文件。

### 用户请求

```
用户：继续为 user 模块生成文档
```

### 执行流程

#### 1. 检测到不一致

```

📋 阶段1：验证文件索引

检查索引文件: kb/user/.file-list.md ... ✅ 存在

验证完整性...
  - 索引记录数: 45
  - 实际文件数: 48 ⚠️ 不一致（+3 新文件）

⚠️ 检测到文件变化，需要重新创建索引

新增文件:
  1. controller/MenuController.java
  2. service/MenuService.java
  3. mapper/MenuMapper.java

✅ 自动重新创建索引，保留已生成的文档状态

```

#### 2. 重新创建索引（保留已完成状态）

```

📂 重新扫描模块

扫描到的文件数: 48 个

同步已生成的文档状态:
  - 已生成文档: 28 个 ✅
  - 新增文件: 3 个（状态：待创建）
  - 未完成文件: 17 个（状态：待创建）

创建新索引: kb/user/.file-list.md ✅
  - 总记录数: 48
  - 已完成: 28
  - 待处理: 20

✅ 索引文件更新完成

```

#### 3. 继续处理未完成的文件

```

📄 处理第10批（文件29-31）

文件29,30,31... 

生成文档...\n  

🔄 立即更新索引文件（批量更新文件29-31）...
  保存索引: kb/user/.file-list.md ✅

✅ 第10批（文件29-31）处理完成

（自动继续处理剩余文件...）
```

#### 4. 阶段3：生成目录索引与完成总结

**步骤3.1：询问是否生成 README.md**

```
📋 阶段3：生成目录索引

所有 48 个文件的文档已生成完成！

是否为文档目录生成 README.md 索引文件？

选项：
  [1] 生成 README.md（如已存在则更新）
  [2] 跳过

请选择（1/2）：
```

> **🔴 此时必须停止并等待用户选择！**

（用户选择后执行相应操作）

**步骤3.3：输出完成总结**

```
🎉 文档生成完成

模块: src/main/java/com/xxx/user
文档目录: kb/user/

📊 统计信息:
  - 总文件数: 48
  - 已生成文档: 48（本次新增20个）
  - README.md: ✅ 已生成 / ⏭️ 已跳过

✅ 所有文档已保存到 kb/user/ 目录
```

---

## 示例4：指定自定义文档目录

### 用户请求

```
用户：为 src/main/java/com/xxx/order 模块生成文档，保存到 docs/api
```

### 执行流程

```

🎯 参数确认

源代码目录: src/main/java/com/xxx/order ✅
文档输出目录: docs/api ✅（用户指定）

✅ 开始执行

```

（后续流程与示例1相同，包括阶段2按3个一批生成文档、阶段3先询问是否生成README.md再输出完成总结，只是输出到 docs/api/ 目录）

---

## 常见问题

### Q1: 如果某个文件生成失败怎么办？

**A**: 文件生成失败时，状态会保持为"待创建"，索引文件不会更新。系统会自动跳过该文件继续处理下一个，最后在总结中列出所有失败的文件。

```

❌ 文件 15 处理失败

代码文件: service/ComplexService.java
错误原因: 文件过大，无法完整读取

⚠️ 已记录失败信息，自动继续处理下一批文件

```

### Q2: 可以同时为多个模块生成文档吗？

**A**: 每次只能为一个模块生成文档。如需为多个模块生成，请分别执行。

### Q3: 生成的文档可以自定义格式吗？

**A**: 可以修改 `templates/` 目录下的模板文件来自定义格式。

### Q4: 如何查看当前进度？

**A**: 查看 `{DOC_DIR}/.file-list.md` 文件，其中包含完整的进度信息。

---

## 输出文件示例

### 索引文件（.file-list.md）

```markdown
# 文件索引

> **模块路径**: src/main/java/com/xxx/user  
> **文档目录**: kb  
> **创建时间**: 2025-12-11 14:30:00  
> **最后更新**: 2025-12-11 15:45:00

---

## 📊 统计信息

| 指标 | 数值 |
|------|------|
| 总文件数 | 45 |
| 已生成文档 | 45 |
| 待处理文件 | 0 |
| 完成进度 | 100% |

---

## 📋 文件清单

| 序号 | 代码文件路径 | 文件大小 | 文档文件路径 | 文档大小 | 状态 |
|------|--------------|----------|--------------|----------|------|
| 1 | controller/UserController.java | 3.2KB | controller/UserController.md | 5.8KB | 已生成 |
| 2 | controller/RoleController.java | 2.8KB | controller/RoleController.md | 4.2KB | 已生成 |
| ... | ... | ... | ... | ... | ... |
```

### 生成的文档示例（UserController.md）

```markdown
# UserController

> **路径**: `com.xxx.user.controller.UserController`  
> **类型**: Controller  
> **API 风格**: RESTful  
> **基础路径**: `/api/users`  
> **职责**: 用户管理相关的 REST API 端点  
> **代码行数**: 156 行（SLOC）

---

## 📊 统计信息

| 接口总数 | GET | POST | PUT/PATCH | DELETE | 依赖服务数 |
|:--------:|:---:|:----:|:---------:|:------:|:----------:|
| 8 | 3 | 2 | 2 | 1 | 2 |

---

## 📋 完整接口列表

### 接口清单

| # | 方法名 | HTTP | 路径 | 分页 | 事务 | 说明 |
|:-:|--------|------|------|:----:|:----:|------|
| 1 | `getUserById` | GET | `/api/users/{id}` | - | - | 根据ID查询用户 |
| 2 | `listUsers` | GET | `/api/users` | ✓ | - | 分页查询用户列表 |
| 3 | `createUser` | POST | `/api/users` | - | ✓ | 创建新用户 |
| 4 | `updateUser` | PUT | `/api/users/{id}` | - | ✓ | 更新用户信息 |
| 5 | `deleteUser` | DELETE | `/api/users/{id}` | - | ✓ | 删除用户 |
| ... | ... | ... | ... | ... | ... | ... |

---

### getUserById

| 属性 | 值 |
|------|-----|
| HTTP | `GET` `/api/users/{id}` |
| 签名 | `Result<UserVO> getUserById(Long id)` |
| 权限 | `hasRole('USER')` |
| 说明 | 根据用户ID查询用户详细信息 |

**参数**:

| 参数 | 类型 | 注解 | 必填 | 说明 |
|------|------|------|:----:|------|
| id | `Long` | `@PathVariable` | ✓ | 用户ID |

**返回**: `Result<UserVO>` - 用户详情

---

### createUser

| 属性 | 值 |
|------|-----|
| HTTP | `POST` `/api/users` |
| 签名 | `Result<Long> createUser(CreateUserDTO dto)` |
| 权限 | `hasRole('ADMIN')` |
| 说明 | 创建新用户 |

**参数**:

| 参数 | 类型 | 注解 | 必填 | 说明 |
|------|------|------|:----:|------|
| dto | `CreateUserDTO` | `@RequestBody @Valid` | ✓ | 用户创建请求 |

**返回**: `Result<Long>` - 新创建的用户ID

---

## 🔗 依赖组件（我依赖谁）

| 组件 | 类型 | 用途 |
|------|------|------|
| UserService | Service | 用户业务逻辑处理 |
| RoleService | Service | 角色权限查询 |

---

## 🔄 Service 调用映射

| 接口方法 | 调用 Service | 调用方法 | 说明 |
|----------|--------------|----------|------|
| `getUserById` | `UserService` | `findById(Long)` | 查询用户详情 |
| `createUser` | `UserService` | `create(CreateUserDTO)` | 创建用户 |
| `updateUser` | `UserService` | `update(Long, UpdateUserDTO)` | 更新用户 |

---

## 📎 关联文档

| 文档类型 | 文档名称 | 说明 |
|----------|----------|------|
| Service | [UserService](./service/UserService.md) | 用户业务服务 |
| DTO | [CreateUserDTO](./dto/CreateUserDTO.md) | 用户创建请求 |
| VO | [UserVO](./vo/UserVO.md) | 用户响应对象 |

---

## 📝 业务规则

- 用户名长度 3-20 字符，仅允许字母数字下划线
- 邮箱格式校验，且全局唯一
- 创建用户时自动分配默认角色

---

## 📝 维护记录

| 时间 | 维护人 | 维护内容 | 版本 |
|------|--------|----------|------|
| 2025-12-11 | AI自动生成 | 初始创建文档 | v1.0 |
```
